// Shared constants for ListingsViewer table layout
export const IMAGE_CELL_WIDTH = 116; // image square width/height
export const IMAGE_CONTENT_GAP = 12; // gap between image and first column
export const IMAGE_CELL_TOTAL = IMAGE_CELL_WIDTH + IMAGE_CONTENT_GAP; // total horizontal space consumed
export const COLUMN_INNER_PADDING = 8; // horizontal padding inside each column cell for content

export default {
  IMAGE_CELL_WIDTH,
  IMAGE_CONTENT_GAP,
  IMAGE_CELL_TOTAL,
};
